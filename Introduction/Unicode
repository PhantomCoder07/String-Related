#include <stdio.h>
int main()
{
    char str[100];
    printf ("Enter the string: ");
    gets (str);
    for (int i=0; str[i]!='\0'; i++)
    {
        printf("The Unicode Code Point At %d is: %d\n",i,str[i]);
    }
    return 0;
}
