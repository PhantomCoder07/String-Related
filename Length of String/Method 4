#include <stdio.h>
int strlen (char *s)
{
    int len=0;
    while (*s++)
    {
        len++;
    }
    return len;
}
int main()
{
    char str[100];
    printf("Enter string: ");
    gets(str);
    printf("Length of the string: %d\n",strlen(str));
    return 0;
}
