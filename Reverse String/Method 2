#include <stdio.h>
#include <string.h>
int main()
{
    char str[100];
    int i,l;
    printf ("Enter the string: ");
    gets (str);
    l=strlen(str);
    printf ("The reverse string:");
    for (i=l; i>=0; i--)
    {
        printf ("%c",str[i]);
    }
    return 0;
}
